#ifndef WIFI_MANAGER_H
#define WIFI_MANAGER_H

class WiFiManager {
public:
    // Initialize Wi-Fi stack and connect using the hard-coded credentials (same as your code)
    void init();
};

#endif // WIFI_MANAGER_H
